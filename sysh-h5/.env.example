# 域名配置
DOMAIN=your-domain.com

# Let's Encrypt 证书目录
CERT_DIR=/etc/letsencrypt

# 端口配置 (可选)
HTTP_PORT=80
HTTPS_PORT=443

# 前端构建时使用的后端 API 地址
VITE_API_BASE_URL=https://sysh.tennis168.vip/api

# Docker 旧代理配置保留项（当前 server 不再使用代理）
TARGET_API=https://sysh.tennis168.vip
